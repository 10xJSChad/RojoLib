#include <string> 
#include <vector>

#ifndef ROJOLIB_H
#define ROJOLIB_H

std::vector<std::string> split(std::string s, char splitAt);
std::string strip(std::string s, char toStrip);

#endif
